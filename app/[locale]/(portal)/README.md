# (portal) — reserved route group

Reserved for a future customer space (order tracking, weighbridge tickets,
invoices via the group's internal systems, Entra External ID). **Reserve only —
do not build** (master prompt §9, question 10 default). No `page.tsx` here, so
nothing routes.
